/*
Operadores Lógicos
&& -> AND -> E
|| -> OR -> OU
! -> NOT -> NÃO negação pouco utilizado
*/
const expressaoAnd = true && true;// todas as expressões precisam ser verdadeiras
const expressaoOr = false || false; // se qualquer expressão retornar true, a espressão por completo será true. Caso as duas retorne false, a expressão retornará false por completo.
console.log(expressaoOr);
